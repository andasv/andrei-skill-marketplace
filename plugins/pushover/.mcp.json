{
  "pushover": {
    "command": "python",
    "args": ["-m", "pushover_mcp"],
    "cwd": "${CLAUDE_PLUGIN_ROOT}",
    "env": {
      "PUSHOVER_APP_TOKEN": "${PUSHOVER_APP_TOKEN}",
      "PUSHOVER_USER_KEY": "${PUSHOVER_USER_KEY}"
    }
  }
}
